# 🐺 LiveSync Hub - Change Log (change.md)

## ＜変更点＞

### 1. ホーム画面の役割変更（Dashboard化）
* **変更前:** ホーム画面を「統合チャット画面」として定義。
* **変更後:** ホーム画面を **「Dashboard（ダッシュボード）」** の立ち位置に変更。
* **理由:** アプリの特性上、多くのサービスが一箇所に集中するため、チャットだけではなく各機能へのアクセス性やステータス確認を優先し、操作性を向上させるため。
* **構成要素:** * 接続中のプラットフォームステータス
    * 次の配信スケジュール
    * アクティブな参加型イベントへのショートカット
    * 簡易チャットプレビュー

### 2. ログインフローの最適化
* **変更内容:** **ログイン画面を最優先で表示**するよう変更。
* **表示ルール:** * 下部タブバー（Bottom Tab Bar）にはログイン項目を表示しない。
    * アプリ起動時、未認証状態であれば必ずログインをリクエストする。
    * 毎回のセッションにおいて、セキュリティとデータ整合性のために確実な認証状態を確保する。
* **ロジック:** `/scripts/auth-fetch.ts` と `/app/login.tsx` を連携させ、認証が完了するまでメイン機能（Dashboard）へのアクセスを制限する。