# 🐺 LiveSync Hub - Change Log (change.md)

## ＜変更点＞

### 1. Dashboard の構成（モード選択画面）
ホーム画面（Dashboard）を、ユーザーの利用シーンに合わせた「モード選択ハブ」として再設計します。

* **画面上部表示:** * `LiveSync Hub` (ロゴ/タイトル)
    * `ようこそ、〇〇さん` (ユーザー名表示)
* **選択可能なモード:**
    1.  **[Liveモード]**: `Live参加コードを入力`（入力フォームへ）
    2.  **[会場モード]**: `会場アクセス端末の接続待機中`（ホスト・会場向け待機画面）
    3.  **[Normalモード]**: `動画コードを入力`（アーカイブ・動画用入力フォームへ）
* **フッターアクション:**
    * `[ログアウト]` ボタン

### 2. モード選択後の表示（実行画面）
モード選択およびコード入力後は、以下の情報をヘッダーエリアに固定表示し、その下に各サービスのメイン画面（チャット等）を表示します。

* **ステータス表示:**
    * `LiveSync Hub`
    * `現在のモード：[選択したモード]`
    * `接続した部屋：[部屋名 / 動画名 / 会場名]`
* **メインコンテンツ:**
    * 各種プラットフォームの統合チャットや参加型ゲーム画面を表示。

### 3. デザイン方針（ヘッダーレス & 没入型）
* **構成:** 全画面 `headerShown: false`。
* **背景:** `color1` (#6892ad) から `color2` (#e28862) のグラデーションを背景またはアクセントに適用。
* **UI:** Expo API を活用した、シンプルかつ直感的なボタンレイアウト。